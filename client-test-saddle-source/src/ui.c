#include "../include/ui.h"

int run_ui(struct state_minor *state, struct client *client)
{
    // Clear the screen
    
    // Print a big ass menu
    
    // Take user input
    
    return 0;
}

int display_results(struct state_minor *state)
{
    // Display the results
    
    // Wait for key press
    
    return 0;
}
